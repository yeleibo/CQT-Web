import { createFromIconfontCN } from '@ant-design/icons';
///图标
///eg:    <XDIcon  type="icon-shengpi"></XDIcon>
const XDIcon = createFromIconfontCN({
  scriptUrl: '/icons/iconfont.js', // 在 iconfont.cn 上生成
});

export default XDIcon;
